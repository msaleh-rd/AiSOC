{
  "name": "@isoc/action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action that triages a repo's own security signals (Dependabot, CodeQL, secret scanning) with the Intelligence SOC deterministic verdict engine.",
  "license": "MIT",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@isoc/report-card": "workspace:*",
    "@types/node": "^26.1.1",
    "tsup": "^8.3.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
